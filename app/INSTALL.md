# Install DOBO Textured Planters Phase 2

Extract directly into:

```text
E:\dobo-engine
```

This package only adds:

```text
app\product_collections\textured_planters\
```

No Kernel files are replaced.

Then run:

```powershell
python -m product_collections.textured_planters.test_phase_2
```
